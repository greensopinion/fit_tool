/// Autogenerated. Do not modify.
///
/// Profile: 21.60
import 'dart:typed_data';

import '../../base_type.dart';
import '../../data_message.dart';
import '../../definition_message.dart';
import '../../errors.dart';
import '../../field.dart';
import 'common_fields.dart';

class SoftwareMessage extends DataMessage {
  SoftwareMessage(
      {definitionMessage,
      developerFields,
      int localId = 0,
      Endian endian = Endian.little})
      : growable = definitionMessage == null,
        super(
            name: SoftwareMessage.NAME,
            globalId: SoftwareMessage.ID,
            localId: definitionMessage?.localId ?? localId,
            endian: definitionMessage?.endian ?? endian,
            definitionMessage: definitionMessage,
            developerFields: developerFields,
            fields: [
              MessageIndexField(
                  size: definitionMessage
                          ?.getFieldDefinition(MessageIndexField.ID)
                          ?.size ??
                      0,
                  growable: definitionMessage == null),
              SoftwareVersionField(
                  size: definitionMessage
                          ?.getFieldDefinition(SoftwareVersionField.ID)
                          ?.size ??
                      0,
                  growable: definitionMessage == null),
              SoftwarePartNumberField(
                  size: definitionMessage
                          ?.getFieldDefinition(SoftwarePartNumberField.ID)
                          ?.size ??
                      0,
                  growable: definitionMessage == null)
            ]);

  /// The Global ID of the message. In the FIT documentation this is referred to as the "Global Message Number".
  static const ID = 35;
  static const NAME = 'software';

  final bool growable;

  /// Returns an instance of SoftwareMessage from a bytes list.
  static SoftwareMessage fromBytes(
      DefinitionMessage definitionMessage, Uint8List bytes) {
    final message = SoftwareMessage(definitionMessage: definitionMessage);
    message.readFromBytes(bytes);
    return message;
  }

  /// Returns the value of the messageIndex field. Returns null if the field is not defined in the message.
  int? get messageIndex {
    final field = getField(MessageIndexField.ID);
    if (field != null && field.isValid()) {
      var subField = field.getValidSubField(fields);
      return field.getValue(subField: subField);
    } else {
      return null;
    }
  }

  /// Sets the messageIndex field with [value]. Throws [FieldNotDefinedError] if the field is not defined in the message.
  set messageIndex(int? value) {
    final field = getField(MessageIndexField.ID);

    if (field != null) {
      if (value == null) {
        field.clear();
      } else {
        var subField = field.getValidSubField(fields);
        field.setValue(0, value, subField);
      }
    } else {
      throw FieldNotDefinedError('${field!.name}');
    }
  }

  /// Returns the value of the version field. Returns null if the field is not defined in the message.
  double? get version {
    final field = getField(SoftwareVersionField.ID);
    if (field != null && field.isValid()) {
      var subField = field.getValidSubField(fields);
      return field.getValue(subField: subField);
    } else {
      return null;
    }
  }

  /// Sets the version field with [value]. Throws [FieldNotDefinedError] if the field is not defined in the message.
  set version(double? value) {
    final field = getField(SoftwareVersionField.ID);

    if (field != null) {
      if (value == null) {
        field.clear();
      } else {
        var subField = field.getValidSubField(fields);
        field.setValue(0, value, subField);
      }
    } else {
      throw FieldNotDefinedError('${field!.name}');
    }
  }

  /// Returns the value of the partNumber field. Returns null if the field is not defined in the message.
  String? get partNumber {
    final field = getField(SoftwarePartNumberField.ID);
    if (field != null && field.isValid()) {
      var subField = field.getValidSubField(fields);
      return field.getValue(subField: subField);
    } else {
      return null;
    }
  }

  /// Sets the partNumber field with [value]. Throws [FieldNotDefinedError] if the field is not defined in the message.
  set partNumber(String? value) {
    final field = getField(SoftwarePartNumberField.ID);

    if (field != null) {
      if (value == null) {
        field.clear();
      } else {
        var subField = field.getValidSubField(fields);
        field.setValue(0, value, subField);
      }
    } else {
      throw FieldNotDefinedError('${field!.name}');
    }
  }
}

class SoftwareVersionField extends Field {
  SoftwareVersionField({int size = 0, bool growable = true})
      : super(
            name: 'version',
            id: ID,
            type: BaseType.UINT16,
            offset: 0,
            scale: 100,
            size: size,
            growable: growable,
            subFields: []);

  static const ID = 3;
}

class SoftwarePartNumberField extends Field {
  SoftwarePartNumberField({int size = 0, bool growable = true})
      : super(
            name: 'part_number',
            id: ID,
            type: BaseType.STRING,
            offset: 0,
            scale: 1,
            size: size,
            growable: growable,
            subFields: []);

  static const ID = 5;
}
